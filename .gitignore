# Secrets — never commit
.env

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Claude Code — personal settings and stray agent-worktree artifacts
.claude/settings.local.json
.claude/worktrees/

# Data files (raw downloads — regenerate from notebooks)
data/raw/*
!data/raw/.gitkeep

# Trained models (regenerate from notebooks), except the production model
# and its companion feature artifact, which the Streamlit dashboard needs
# at deploy time and are committed directly (see notebook 17 / README).
models/*
!models/production_random_forest.joblib
!models/weekend_weekday_ratio.csv

# Throwaway calibration/benchmark artifacts (notebook 16 Section 7-8's
# out-of-notebook hyperparameter sweep and reference pipeline dump) -
# never part of the tracked repo, regenerate as needed.
.scratch/
